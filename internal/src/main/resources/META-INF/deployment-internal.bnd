# Allows to be used on a 1.6.0 framework even if we use Capabilities/Requirements from OSGi r5
Import-Package org.osgi.framework.*;version=1.6,*

# These libraries are not exposed through OSGi as they're only for internal use
Embed-Dependency osgi-toolkit-filter;inline=true,peergreen-tasks;inline=true